<Window x:Class="WindowsFileManagerPro.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsFileManagerPro.Views"
        mc:Ignorable="d"
        Title="Windows File Manager Pro" 
        Height="800" Width="1200"
        MinHeight="600" MinWidth="800"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="MenuStyle" TargetType="Menu">
            <Setter Property="Background" Value="{DynamicResource MenuBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource MenuForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}"/>
        </Style>
        
        <Style x:Key="ToolBarStyle" TargetType="ToolBar">
            <Setter Property="Background" Value="{DynamicResource ToolBarBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ToolBarForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ToolBarBorderBrush}"/>
        </Style>
        
        <Style x:Key="StatusBarStyle" TargetType="StatusBar">
            <Setter Property="Background" Value="{DynamicResource StatusBarBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource StatusBarForeground}"/>
        </Style>
    </Window.Resources>
    
    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top" Style="{StaticResource MenuStyle}">
            <MenuItem Header="_File">
                <MenuItem Header="_New Window" InputGestureText="Ctrl+N" Click="NewWindow_Click"/>
                <Separator/>
                <MenuItem Header="_Open" InputGestureText="Ctrl+O" Click="Open_Click"/>
                <MenuItem Header="_Save" InputGestureText="Ctrl+S" Click="Save_Click"/>
                <Separator/>
                <MenuItem Header="_Exit" Click="Exit_Click"/>
            </MenuItem>
            
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo" InputGestureText="Ctrl+Z" Click="Undo_Click"/>
                <MenuItem Header="_Redo" InputGestureText="Ctrl+Y" Click="Redo_Click"/>
                <Separator/>
                <MenuItem Header="_Cut" InputGestureText="Ctrl+X" Click="Cut_Click"/>
                <MenuItem Header="_Copy" InputGestureText="Ctrl+C" Click="Copy_Click"/>
                <MenuItem Header="_Paste" InputGestureText="Ctrl+V" Click="Paste_Click"/>
                <Separator/>
                <MenuItem Header="_Find" InputGestureText="Ctrl+F" Click="Find_Click"/>
                <MenuItem Header="_Replace" InputGestureText="Ctrl+H" Click="Replace_Click"/>
            </MenuItem>
            
            <MenuItem Header="_View">
                <MenuItem Header="_Dark Theme" Click="DarkTheme_Click"/>
                <MenuItem Header="_Light Theme" Click="LightTheme_Click"/>
                <Separator/>
                <MenuItem Header="_Tree View" Click="TreeView_Click"/>
                <MenuItem Header="_List View" Click="ListView_Click"/>
                <MenuItem Header="_Details View" Click="DetailsView_Click"/>
            </MenuItem>
            
            <MenuItem Header="_Tools">
                <MenuItem Header="_Search" InputGestureText="Ctrl+Shift+F" Click="Search_Click"/>
                <MenuItem Header="_ZIP Manager" Click="ZipManager_Click"/>
                <MenuItem Header="_Backup Manager" Click="BackupManager_Click"/>
            </MenuItem>
            
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Click="About_Click"/>
                <MenuItem Header="_Documentation" Click="Documentation_Click"/>
            </MenuItem>
        </Menu>
        
        <!-- Toolbar -->
        <ToolBar DockPanel.Dock="Top" Style="{StaticResource ToolBarStyle}">
            <Button Content="New Window" Click="NewWindow_Click" ToolTip="Open new window (Ctrl+N)"/>
            <Separator/>
            <Button Content="Open" Click="Open_Click" ToolTip="Open file (Ctrl+O)"/>
            <Button Content="Save" Click="Save_Click" ToolTip="Save file (Ctrl+S)"/>
            <Separator/>
            <Button Content="Cut" Click="Cut_Click" ToolTip="Cut (Ctrl+X)"/>
            <Button Content="Copy" Click="Copy_Click" ToolTip="Copy (Ctrl+C)"/>
            <Button Content="Paste" Click="Paste_Click" ToolTip="Paste (Ctrl+V)"/>
            <Separator/>
            <Button Content="Delete" Click="Delete_Click" ToolTip="Delete (Del)"/>
            <Button Content="Rename" Click="Rename_Click" ToolTip="Rename (F2)"/>
            <Separator/>
            <Button Content="Search" Click="Search_Click" ToolTip="Search (Ctrl+Shift+F)"/>
            <Button Content="Refresh" Click="Refresh_Click" ToolTip="Refresh (F5)"/>
        </ToolBar>
        
        <!-- Main Content -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - File Explorer -->
            <Border Grid.Column="0" 
                    Background="{DynamicResource TreeViewBackground}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,1,0">
                <local:FileExplorerView x:Name="FileExplorer"/>
            </Border>
            
            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Stretch"
                          Background="{DynamicResource SplitterBackground}"
                          Width="5"/>
            
            <!-- Right Panel - Editor/Content -->
            <Border Grid.Column="2" 
                    Background="{DynamicResource EditorBackground}">
                <TabControl x:Name="MainTabControl" 
                           Background="Transparent"
                           BorderThickness="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Background" Value="{DynamicResource TabBackground}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TabForeground}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource TabBorderBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1,1,1,0">
                                            <ContentPresenter ContentSource="Header"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"
                                                            Margin="8,4"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource TabSelectedBackground}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource TabSelectedForeground}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TabControl.Resources>
                    
                    <!-- Welcome Tab -->
                    <TabItem Header="Welcome">
                        <Grid Background="{DynamicResource EditorBackground}">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="🚀 Windows File Manager Pro" 
                                           FontSize="24" 
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,20"/>
                                <TextBlock Text="Professional file management with built-in editor" 
                                           FontSize="16"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,40"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Open Folder" 
                                            Click="OpenFolder_Click"
                                            Margin="10,0"
                                            Padding="20,10"/>
                                    <Button Content="New File" 
                                            Click="NewFile_Click"
                                            Margin="10,0"
                                            Padding="20,10"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>
        
        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" Style="{StaticResource StatusBarStyle}">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="FileCountText" Text="0 files"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="PathText" Text=""/>
            </StatusBarItem>
        </StatusBar>
    </DockPanel>
</Window>