<UserControl x:Class="WindowsFileManagerPro.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource EditorBackground}">
    
    <UserControl.Resources>
        <Style x:Key="LineNumberStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource EditorLineNumbersForeground}"/>
            <Setter Property="Background" Value="{DynamicResource EditorLineNumbersBackground}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="5,2"/>
        </Style>
        
        <Style x:Key="EditorTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource EditorBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource EditorForeground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="AcceptsTab" Value="True"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="UndoLimit" Value="100"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource EditorSelectionBackground}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource EditorForeground}"/>
        </Style>
        
        <Style x:Key="ToolBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonPressedBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <DockPanel>
        <!-- Editor Toolbar -->
        <ToolBar DockPanel.Dock="Top" 
                 Background="{DynamicResource ToolBarBackground}"
                 BorderBrush="{DynamicResource ToolBarBorderBrush}">
            <Button Content="Save" 
                    Click="SaveButton_Click"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="Save file (Ctrl+S)"/>
            <Button Content="Save As" 
                    Click="SaveAsButton_Click"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="Save file as..."/>
            <Separator/>
            <Button Content="Undo" 
                    Click="UndoButton_Click"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="Undo (Ctrl+Z)"/>
            <Button Content="Redo" 
                    Click="RedoButton_Click"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="Redo (Ctrl+Y)"/>
            <Separator/>
            <Button Content="Find" 
                    Click="FindButton_Click"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="Find (Ctrl+F)"/>
            <Button Content="Replace" 
                    Click="ReplaceButton_Click"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="Replace (Ctrl+H)"/>
            <Separator/>
            <Button Content="Format" 
                    Click="FormatButton_Click"
                    Style="{StaticResource ToolBarButtonStyle}"
                    ToolTip="Format code"/>
            <Separator/>
            <TextBlock Text="Language:" 
                       VerticalAlignment="Center" 
                       Margin="10,0"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
            <ComboBox x:Name="LanguageComboBox" 
                      Width="120"
                      SelectionChanged="LanguageComboBox_SelectionChanged"/>
            <Separator/>
            <CheckBox x:Name="LineNumbersCheckBox" 
                      Content="Line Numbers" 
                      IsChecked="True"
                      Checked="LineNumbersCheckBox_Checked"
                      Unchecked="LineNumbersCheckBox_Unchecked"
                      VerticalAlignment="Center"
                      Foreground="{DynamicResource TextPrimaryBrush}"/>
            <CheckBox x:Name="WordWrapCheckBox" 
                      Content="Word Wrap" 
                      IsChecked="False"
                      Checked="WordWrapCheckBox_Checked"
                      Unchecked="WordWrapCheckBox_Unchecked"
                      VerticalAlignment="Center"
                      Foreground="{DynamicResource TextPrimaryBrush}"/>
        </ToolBar>
        
        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" 
                   Background="{DynamicResource StatusBarBackground}"
                   Foreground="{DynamicResource StatusBarForeground}">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="PositionText" Text="Line 1, Column 1"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="EncodingText" Text="UTF-8"/>
            </StatusBarItem>
        </StatusBar>
        
        <!-- Main Editor Area -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Line Numbers -->
            <ScrollViewer x:Name="LineNumbersScrollViewer" 
                          Grid.Column="0"
                          Background="{DynamicResource EditorLineNumbersBackground}"
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Hidden">
                <TextBlock x:Name="LineNumbersTextBlock" 
                           Style="{StaticResource LineNumberStyle}"
                           Text="1"/>
            </ScrollViewer>
            
            <!-- Text Editor -->
            <ScrollViewer x:Name="EditorScrollViewer" 
                          Grid.Column="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="EditorTextBox"
                         Style="{StaticResource EditorTextBoxStyle}"
                         TextChanged="EditorTextBox_TextChanged"
                         SelectionChanged="EditorTextBox_SelectionChanged"
                         KeyDown="EditorTextBox_KeyDown"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         UndoLimit="100"/>
            </ScrollViewer>
        </Grid>
    </DockPanel>
</UserControl>